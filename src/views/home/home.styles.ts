import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    noData: {
        fontSize: 16,
        textAlign: 'center'
    },
    loading: {
        marginTop: 5
    }
})
export default styles;